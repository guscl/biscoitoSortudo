<%= form_for(@fortune_message) do |f| %>
  <% if @fortune_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fortune_message.errors.count, "error") %> prohibited this fortune_message from being saved:</h2>

      <ul>
      <% @fortune_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
